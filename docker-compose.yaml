version: '3.8'
services:
  postgres:
    image: postgres:15.2
    ports:
      - 5433:5433
    environment:
      POSTGRES_DB: be_lesson
      POSTGRES_USER: andrej
      POSTGRES_PASSWORD: heslo
    command: -p 5433
